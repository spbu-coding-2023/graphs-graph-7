package algorithms

import graph.model.Graph

class BridgeFinder<V>(graph: Graph<V>) {
    val visitedVertices = mutableListOf<V>()
    val timeIn = mutableListOf<V>()
    val fUp = mutableListOf<V>()
    val timer = 0L
    fun findBridges(){
        fun dfs(){

        }

        for (i in ){

        }
    }

}